# 06 · Claude Code 작업 단계와 프롬프트

각 단계는 "완료 조건"이 전부 통과해야 다음으로 간다. 진행하며 체크박스를 갱신한다.
프롬프트는 VS Code의 Claude Code 세션에 그대로 붙여 넣는다. 첫 세션 시작 시 항상 `CLAUDE.md`와 docs를 읽게 한다.

---

## 0단계 · 스캐폴드
```
CLAUDE.md와 docs/00, 02, 03을 읽고 시작하세요.
Next.js 15(App Router, TypeScript strict) 프로젝트를 만들고 next.config.ts에 output:'export', images.unoptimized를 설정하세요.
Tailwind와 shadcn/ui(stone 프리셋, radius 0.5rem)를 설치하고 다음 컴포넌트를 추가하세요:
button badge card tabs slider switch dialog sheet table tooltip separator toggle-group progress collapsible alert chart sonner
zustand, recharts, vitest, @types/w3c-web-serial, lz-string을 설치하세요.
globals.css에 docs/03 §3 토큰과 .num 클래스를 넣고 app/layout.tsx head에 Pretendard CDN을 로드하세요.
app/page.tsx에 헤더(로고·제목·상태 배지·버튼 4개)와 탭 4개(실험실·시뮬레이터·탐구·기록) 껍데기를 만들고,
navigator.serial이 없으면 상단에 Alert로 "Chrome 또는 Edge에서 열어 주세요. 지금은 모의 모드로 볼 수 있어요"를 띄우세요.
docs/02 §5의 파일 구조대로 빈 파일을 만들어 두세요.
```
- [x] `npm run build` 통과, `out/` 생성
- [ ] 헤더·탭이 docs/03 §5.1~5.2대로 보임(이모지·영문 없음) — docs/03이 없어 확인 보류. 현재는 CLAUDE.md 금지 목록만 지킨 껍데기

## 1단계 · 계산 코어 + 테스트
```
docs/04-계산-사양.md를 읽고 lib/shadow.ts, lib/solar.ts, lib/calibration.ts를 구현하세요.
브라우저·React 의존 없는 순수 함수여야 합니다. 시그니처는 문서 그대로 따르세요.
tests/shadow.test.ts, tests/solar.test.ts에 문서의 테스트 벡터를 전부 넣고 통과시키세요.
findBoundary의 NaN 건너뛰기, nolight/inside/beyond 분기를 빠뜨리지 마세요.
```
- [x] `npm test` 전부 통과 (64개)
- [x] 제주 하지 79.9° / 동지 33.1° / 9월 12일 59.9°

## 2단계 · 시리얼 + 파서 + 모의 모드 + store
```
docs/01과 docs/02 §5.1을 읽으세요. docs/reference/바이브코딩_웹시리얼연결_기본.html의 통신 로직을
lib/serial.ts의 EzmakerSerial 클래스로 이식하세요(baudRate 115200, LineBreakTransformer, cancel→inputDone 대기→releaseLock 순서 유지).
추가: navigator.serial 'disconnect' 이벤트 처리, getPorts()로 재연결, 상태 콜백.
lib/parser.ts(parseLine), lib/mock.ts(docs/01 사양의 삼각파+노이즈+구름, 수동 입력 생성기)를 구현하세요.
store/useStore.ts(zustand + persist): live(최근 reading, 중앙값, 정규값, boundary), cal, cfg(DEFAULT_DEVICE), snapshots, predictions, settings(관측지, 프로젝터 모드), serialState.
헤더 버튼을 연결하세요: 장치 연결/해제, 수신 시작/중지, 모의 스위치. 모의 모드는 같은 onLine 경로를 씁니다.
개발자 콘솔에 2 Hz로 파싱 결과가 찍히면 완료입니다.
```
- [ ] 실기기: 연결→시작→중지→해제→연결→시작 3회 반복, 에러 없음
- [ ] 케이블 분리 시 상태 배지 "연결 끊김", [연결] 재시도 동작
- [ ] 모의 모드에서 boundary.kind가 ok/beyond/inside/nolight 모두 나타남

## 3단계 · 실험실 탭
```
docs/03 §5.4~5.5, §5.6을 읽고 components/lab/BigNumbers.tsx, SensorStrip.tsx, LiveChart.tsx, ManualInput.tsx와
components/connect/CalibrationDialog.tsx를 구현하세요.
BigNumbers: 카드 3장(태양 고도·그림자 길이·빛 에너지), 48px .num, 태양 고도 카드만 좌측 앰버 띠와 신뢰도 배지.
SensorStrip: 5행, Progress(정규값), ADC 원값, 상태 텍스트, 경계 쌍 ring-2 ring-amber-500, Collapsible '계산 보기'에 보간식.
LiveChart: shadcn ChartContainer + recharts ComposedChart, 좌축 고도 amber-500, 우축 그림자 slate-600, 최근 120점, 구간 토글 1분·5분·전체, [스냅샷] [CSV].
CalibrationDialog: docs/01 보정 절차 2단계, 대비 부족 경고.
ManualInput: 그림자 길이 또는 고도 입력 → mock 생성기로 흘리기.
모의 모드로 값이 변할 때 세 카드·센서 띠·차트가 함께 움직여야 합니다.
```
- [ ] 조명 30°→70° 이동 시 고도 표시 ±3° 안(각도기 대조)
- [ ] 보정 후 신뢰도 배지가 "높음"으로 바뀜
- [ ] 스냅샷 저장 → 기록 탭 표에 나타남

## 4단계 · 하늘 장면
```
docs/03 §5.3의 좌표 규칙대로 components/lab/SkyScene.tsx를 순수 SVG(viewBox 0 0 720 420)로 구현하세요.
하늘색 3단계 단색, 고도 호 10° 눈금, 태양(반지름 14 + 22 앰버 20%), 광선 점선, 가림판, 그림자 띠, 반그림자, 센서 5개, 상태 문구, 구름 대체.
태양 위치는 300ms transition. 하단에 계절 프리셋 ToggleGroup(하지·오늘 측정·춘추분·동지): 프리셋 선택 시 회색 태양이 이론 위치에 추가로 뜹니다.
그림자 17.8cm일 때 경계가 S3(21.0)와 S2(14.4) 사이에 그려지는지 확인하세요.
```
- [ ] 비례 정확(센서 위치와 그림자 끝이 cm 눈금과 일치)
- [ ] nolight에서 구름 + "빛을 찾지 못했어요"

## 5단계 · 유도값 패널 + 시뮬레이터
```
docs/04 §C 매핑대로 components/lab/DerivedPanel.tsx(추론 카드 4장 + Sheet로 전체 표)를 구현하세요.
docs/02 §3.2, docs/03 §5.9대로 components/sim/Simulator.tsx, DayCurveChart.tsx, Presets.tsx를 구현하세요.
슬라이더: 날짜·시각·위도·경도·가림판 높이. 프리셋: 제주·서울·시드니·적도, 하지·동지·춘분.
[실측 겹치기]를 누르면 오늘 스냅샷 점들이 이론 곡선 위에 표시됩니다. 실시간 토글은 PC 시계를 씁니다.
```
- [ ] 시뮬레이터 제주 하지 79.9° / 동지 33.1°
- [ ] 스냅샷이 곡선 위에 겹침
- [ ] 유도값 18개 중 입력 부족 항목은 흐리게 + 안내

## 6단계 · 탐구 탭
```
docs/05를 읽고 data/inquiryCards.ts에 18개 카드를 정의하세요(14번 hidden).
components/inquiry/InquiryGallery.tsx, InquiryCard.tsx, Stepper.tsx, PredictLoop.tsx와
tools/{LatitudeCalc,HeightCalc,PanelCalc,CircumferenceCalc,Sundial,Misconception}.tsx를 구현하세요.
잠금 규칙은 docs/05의 needs 매핑을 store에서 계산합니다. 잠긴 카드는 opacity-50, 클릭 시 sonner로 해제 조건 안내.
data/seed.ts에 제주 하지·동지 시뮬 곡선과 예시 스냅샷 6개를 넣고, 첫 실행 시 "예시 데이터 불러오기" 버튼으로만 주입하세요(자동 주입 금지).
```
- [ ] 카드 1·4·9·11·12·13·16 동작 확인
- [ ] 예측 입력 → 다음 스냅샷 매칭 → 오차 표시

## 7단계 · 기록·공유·인쇄·프로젝터·배포
```
components/records/*를 구현하세요: SessionTable, CSV 내보내기/가져오기(docs/02 §3.4 형식), JSON 가져오기(타교 파일, schoolCode 유지),
ShareLink(lz-string으로 스냅샷 요약을 URL 해시에 압축, 열면 가져오기 제안), PrintSheet(@media print: 헤더·탭·버튼 숨김, A4 1장).
헤더에 프로젝터 모드 토글(큰 숫자 64px, 추론 카드 숨김)을 추가하세요.
푸터에 성취기준 문구와 학교명 설정을 넣으세요.
npm run build 후 out/을 확인하고 README.md에 Vercel·Netlify 배포 절차를 적으세요.
```
- [ ] CSV 내보내기→가져오기 왕복, 공유 링크 왕복
- [ ] 인쇄 미리보기 A4 1장
- [ ] Vercel 배포 URL에서 실기기 연결(HTTPS) 확인

---

## 최종 점검(심사·수업 전)
- [ ] Chrome 외 브라우저: 배너 + 모의 모드 진입
- [ ] 보정 없이 모의 모드로 4탭 전부 시연 가능
- [ ] 실기기 5분 연속 수신 끊김 없음
- [ ] 실측 vs 각도기 참값 ±3°
- [ ] 디자인 금지 목록 위반 없음(그라디언트·글로우·이모지·영문 병기)
- [ ] 문구 전부 초6 한국어

## 자주 나는 문제
| 증상 | 원인 | 조치 |
|---|---|---|
| 두 번째 연결에서 "port is locked" | reader.cancel 후 inputDone 미대기 | docs/02 §5.1 순서 준수 |
| 값이 안 들어옴 | baud 불일치 | 보드·앱 모두 115200 |
| 정규값이 전부 0.3~0.7 | 주변광 과다 | 암실 상자, 재보정 |
| 하늘 장면 태양이 튐 | 중앙값 필터 미적용 | store에서 최근 5개 중앙값 사용 |
| build 실패: window is not defined | 서버 렌더 중 Web Serial 접근 | 'use client' + useEffect 안에서만 navigator 접근 |
