# 04 · 계산 사양과 테스트 벡터

모든 함수는 순수 함수. 각도 인자는 도(°), 반환도 도. 내부에서만 라디안 변환. 날짜는 1월 1일 = 1인 연중 일수 `n`.

## A. `lib/shadow.ts`

```ts
export type DeviceConfig = { plateHeightCm: number; sensorPosCm: number[]; sensorAngleDeg: number[]; threshold: number };
export const DEFAULT_DEVICE: DeviceConfig = {
  plateHeightCm: 25, sensorPosCm: [9.1, 14.4, 21.0, 29.8, 43.3], sensorAngleDeg: [70, 60, 50, 40, 30], threshold: 0.5 };

export type Boundary =
  | { kind: 'ok'; shadowCm: number; altitudeDeg: number; pair: [number, number]; confidence: number }
  | { kind: 'beyond'; maxAltitudeDeg: number }     // 전부 그늘: 고도 < 담당각 최소(30)
  | { kind: 'inside'; minAltitudeDeg: number }     // 전부 빛: 고도 > 담당각 최대(70)
  | { kind: 'nolight' };

export function shadowLength(h: number, altDeg: number): number      // h / tan(alt)
export function altitudeFromShadow(h: number, s: number): number     // atan(h/s)
export function sensorPosForAngle(h: number, angleDeg: number): number
export function findBoundary(n: number[], cfg: DeviceConfig): Boundary
export function relativeEnergy(altDeg: number): number               // sin(alt), 0~1
export function spreadFactor(altDeg: number): number                 // 1/sin(alt)
```

findBoundary 규칙
1. n에 NaN이 있으면 그 센서는 건너뛰고 인접 유효 센서끼리 쌍을 만든다
2. `nolight`: n[0] ≥ thr && (max(n) − min(n)) < 0.2
3. `inside`: 모든 n ≥ thr
4. `beyond`: 모든 n < thr
5. `ok`: 첫 i에 대해 n[i] < thr ≤ n[i+1] → `s = x[i] + (x[i+1]−x[i]) · (thr−n[i]) / (n[i+1]−n[i])`, `confidence = n[i+1]−n[i]`

### 테스트 벡터 (h = 25)
| 입력 | 기대 |
|---|---|
| shadowLength(25, 30) | 43.3 (±0.1) |
| shadowLength(25, 50) | 21.0 |
| shadowLength(25, 70) | 9.1 |
| shadowLength(25, 80) | 4.4 |
| altitudeFromShadow(25, 17.8) | 54.5 (±0.1) |
| findBoundary([0.08,0.19,0.62,0.94,0.96]) | ok, shadowCm 19.16 (±0.05), altitudeDeg 52.5, pair [1,2], confidence 0.43 |
| findBoundary([0.05,0.1,0.2,0.3,0.4]) | beyond, maxAltitudeDeg 30 |
| findBoundary([0.9,0.92,0.95,0.97,0.98]) | inside, minAltitudeDeg 70 |
| findBoundary([0.55,0.6,0.58,0.62,0.6]) | nolight |
| findBoundary([0.1,NaN,0.7,0.9,0.95]) | ok, pair [0,2] (NaN 건너뜀) |
| relativeEnergy(54.5) | 0.814 |
| spreadFactor(54.5) | 1.23 |

## B. `lib/solar.ts`

```ts
export const JEJU = { lat: 33.5, lon: 126.53, tzMeridian: 135 };
export function dayOfYear(d: Date): number
export function declination(n: number): number          // 23.44·sin(360·(284+n)/365)
export function equationOfTime(n: number): number       // 분. B=360(n−81)/364; 9.87sin2B − 7.53cosB − 1.5sinB
export function trueSolarTimeMin(clockMin: number, lon: number, n: number, tzMeridian = 135): number
                                                        // clockMin − (tzMeridian−lon)·4 + EOT
export function hourAngle(trueSolarMin: number): number // 15·(tst/60 − 12)
export function altitude(lat: number, decl: number, H: number): number
                                                        // asin(sinφ sinδ + cosφ cosδ cosH)
export function transitAltitude(lat: number, decl: number): number   // 90 − φ + δ (φ>δ 가정, 결과 0~90 clamp)
export function transitClockMin(lon: number, n: number, tzMeridian = 135): number
                                                        // 720 + (tzMeridian−lon)·4 − EOT
export function dayLengthHours(lat: number, decl: number): number     // 2·acos(−tanφ tanδ)/15, 극야·백야 clamp
export function sunriseSunsetMin(lat: number, lon: number, n: number): [number, number]
export function latitudeFromTransit(hmax: number, decl: number): number   // 90 − hmax + δ
export function timeFromAltitude(alt: number, lat: number, decl: number, pm: boolean): number | null
                                                        // cosH = (sin alt − sinφ sinδ)/(cosφ cosδ); 진태양시(분) 반환
export function dayCurve(lat: number, lon: number, n: number, stepMin = 10): { clockMin: number; alt: number }[]
export function axialTiltFromSolstices(hSummer: number, hWinter: number): number   // (hS − hW)/2
export function earthCircumferenceKm(alt1: number, alt2: number, distanceKm: number): number
                                                        // 360 · d / |alt1 − alt2| (같은 날 같은 시각, 같은 경도 근사)
export function datesForTransitAltitude(hmax: number, lat: number): [number, number] | null   // δ = hmax − 90 + φ → 두 날짜
```

### 테스트 벡터 (제주 lat 33.5, lon 126.53; 오차 ±0.5° / ±3분 / ±0.1h)
| 입력 | 기대 |
|---|---|
| declination(172) 하지 | 23.44 |
| declination(355) 동지 | −23.44 |
| transitAltitude(33.5, 23.44) | 79.9 |
| transitAltitude(33.5, −23.44) | 33.1 |
| transitAltitude(33.5, declination(80)) 춘분 | 56.1 |
| transitAltitude(33.5, declination(255)) 9월 12일 | 59.9 |
| dayLengthHours(33.5, 23.44) | 14.2 |
| dayLengthHours(33.5, −23.44) | 9.8 |
| equationOfTime(255) | 4.6 |
| transitClockMin(126.53, 255) | 749 (12:29) |
| transitClockMin(126.53, 172) | 755 (12:35) |
| altitude at 9/12 10:00 clock (n=255) | 53.2 |
| latitudeFromTransit(59.9, 3.42) | 33.5 |
| axialTiltFromSolstices(79.9, 33.1) | 23.4 |
| earthCircumferenceKm(59.9, 55.9, 445) 제주-강원 | ≈ 40,050 (±500) |
| timeFromAltitude(53.2, 33.5, 3.42, false) → 시계 시각으로 환산 | 10:00 (±3분) |

주의: 남중시각 표시는 "약 12:30 ~ 12:35 사이(계절에 따라)"처럼 균시차를 반영해 보여준다. 기획서 초기 문구의 "12:34"는 균시차 미반영 근사값.

## C. 유도값 목록 → 함수 매핑 (DerivedPanel)
| 표시 항목 | 함수 | 필요 입력 |
|---|---|---|
| 그림자 길이 / 태양 고도 | findBoundary | live |
| 빛 에너지(%) | relativeEnergy | alt |
| 빛이 퍼지는 넓이(배) | spreadFactor | alt |
| 물체 높이 | s_obj · tan(alt) | 사용자 그림자 길이 |
| 태양광 패널 각도(지금/연평균) | 90−alt / lat | alt, lat |
| 지금 몇 시(해시계) | timeFromAltitude | alt, 날짜, 오전/오후 |
| 태양 적위 | declination | 날짜 |
| 남중 고도 예측 | transitAltitude | lat, 날짜 |
| 위도 추정 | latitudeFromTransit | 남중 스냅샷 |
| 남중 시각 | transitClockMin | lon, 날짜 |
| 균시차 | equationOfTime | 날짜 |
| 낮 길이 / 일출 / 일몰 | dayLengthHours, sunriseSunsetMin | lat, lon, 날짜 |
| 하지·동지라면 | transitAltitude(lat, ±23.44) | lat |
| 이 고도가 나오는 날짜 | datesForTransitAltitude | 남중 스냅샷, lat |
| 자전축 기울기 | axialTiltFromSolstices | 두 스냅샷 |
| 지구 둘레 | earthCircumferenceKm | 타교 스냅샷, 거리 |
| 기온 시간차 | 고도 정점 시각 − 기온 정점 시각 | temp 스트림 |
